{
  "name": "lalamove-webhook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "compile": "esbuild src/index.ts --bundle --platform=node --target=node18 --format=cjs --outfile=dist/index.js --external:aws-sdk --minify --tree-shaking=true",
    "lambda:deploy": "npm run compile && cd dist && zip -r ../lambda.zip * && cd .. && aws lambda update-function-code --function-name bt-lalamove-webhook-v2 --zip-file fileb://./lambda.zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/mssql": "^9.1.7",
    "@types/node": "^24.1.0",
    "esbuild": "^0.25.8",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "mssql": "^11.0.1"
  }
}
